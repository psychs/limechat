{
    IBClasses = (
        {
            ACTIONS = {textEntered = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "addr_menu" = id; 
                "channel_menu" = id; 
                "console_base" = id; 
                "console_menu" = id; 
                "info_split" = id; 
                "log_base" = id; 
                "log_menu" = id; 
                "log_split" = id; 
                "member_list" = id; 
                "member_menu" = id; 
                menu = id; 
                "root_split" = id; 
                "server_menu" = id; 
                text = id; 
                tree = id; 
                "tree_menu" = id; 
                "url_menu" = id; 
                window = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = ChatBox; LANGUAGE = ObjC; SUPERCLASS = NSBox; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = MainWindow; LANGUAGE = ObjC; SUPERCLASS = NSWindow; }, 
        {CLASS = MemberListView; LANGUAGE = ObjC; SUPERCLASS = NSTableView; }, 
        {
            ACTIONS = {
                changeStyle = id; 
                onAddChannel = id; 
                onAddServer = id; 
                onAutoOp = id; 
                onCancelReconnecting = id; 
                onChannelProperties = id; 
                onConnect = id; 
                onCopyAddress = id; 
                onCopyServer = id; 
                onCopyUrl = id; 
                onDcc = id; 
                onDeleteChannel = id; 
                onDeleteServer = id; 
                onDisconnect = id; 
                onJoin = id; 
                onLeave = id; 
                onMemberClientInfo = id; 
                onMemberDeop = id; 
                onMemberGiveOp = id; 
                onMemberPing = id; 
                onMemberSendFile = id; 
                onMemberTalk = id; 
                onMemberTime = id; 
                onMemberUserInfo = id; 
                onMemberVersion = id; 
                onMemberWhois = id; 
                onMode = id; 
                onNick = id; 
                onOpenUrl = id; 
                onPaste = id; 
                onPasteMyAddress = id; 
                onPreferences = id; 
                onSearchWeb = id; 
                onServerProperties = id; 
                onServerTree = id; 
                onTopic = id; 
            }; 
            CLASS = MenuController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = ServerTreeView; LANGUAGE = ObjC; SUPERCLASS = NSOutlineView; }, 
        {CLASS = SingleLineFormatter; LANGUAGE = ObjC; SUPERCLASS = NSFormatter; }, 
        {CLASS = Splitter; LANGUAGE = ObjC; SUPERCLASS = NSSplitView; }, 
        {CLASS = TextField; LANGUAGE = ObjC; SUPERCLASS = NSTextField; }
    ); 
    IBVersion = 1; 
}